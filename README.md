Вот лаконичный и структурированный `README.md` для твоего репозитория.

```markdown
# 🏦 Bank Income Prediction Service

AI-сервис для прогнозирования доходов клиентов банка и формирования персональных финансовых рекомендаций. Разработано в рамках хакатона.

Решение состоит из **ML-пайплайна** (обучение модели CatBoost) и **REST API сервиса** (FastAPI + Docker) для интеграции.

## 🚀 Особенности решения (Data Science)

*   **Модель:** CatBoostRegressor (Gradient Boosting).
*   **Валидация:** 5-Fold Cross-Validation (устойчивость к переобучению).
*   **Feature Engineering:**
    *   `income_proxy`: Расчет прокси-дохода через обороты по картам.
    *   `data_completeness`: Индекс заполненности профиля клиента.
    *   Обработка пропусков: Native NaN support + Smart Filling.
*   **Таргет:** Логарифмирование целевой переменной (`log1p`) для сглаживания выбросов.
*   **Интерпретация:** Использование SHAP values для объяснения прогноза.
*   **Метрика WMAE:** ~37 000 - 38 000 руб. (Local Validation).

## 📂 Структура репозитория

```text
.
├── solution.ipynb      # Полный цикл обучения модели (EDA, Preprocessing, Training)
├── submission.csv      # Пример файла с прогнозами
└── income-prediction-service/
    ├── app.py          # Основной код сервиса (FastAPI)
    ├── Dockerfile      # Инструкция для сборки контейнера
    ├── requirements.txt
    ├── model.cbm       # Обученная модель (генерируется в ноутбуке)
    └── features.json   # Конфигурация признаков (генерируется в ноутбуке)
```

## 🛠 Запуск сервиса (Docker)

Для запуска нужен установленный Docker.

**1. Сборка образа**
```bash
cd income-prediction-service
docker build -t income-service .
```

**2. Запуск контейнера**
```bash
docker run -d -p 8000:8000 --name income-predictor income-service
```

Сервис будет доступен по адресу: `http://localhost:8000`

*   **Swagger UI:** [http://localhost:8000/docs](http://localhost:8000/docs) (Интерактивная документация)

## 🔌 API Reference

### POST `/predict`
Получение прогноза дохода, сегмента клиента и объяснения факторов.

**Пример запроса (CURL):**

```bash
curl -X 'POST' \
  'http://localhost:8000/predict' \
  -H 'Content-Type: application/json' \
  -d '{
  "features": {
    "salary_6to12m_avg": 75000,
    "turn_cur_cr_avg_act_v2": 120000,
    "age": 35,
    "gender": 1,
    "total_rur_amt_cm_avg": 50000,
    "hdb_bki_total_max_limit": 500000
  }
}'
```

**Пример ответа:**

```json
{
  "predicted_income": 82500.5,
  "segment": "Middle",
  "shap_explanation": [
    {
      "feature": "turn_cur_cr_avg_act_v2",
      "impact": 0.45,
      "description": "Влияние на прогноз"
    }
  ]
}
```